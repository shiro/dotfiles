import * as React from "react";
import cn from "classnames";

import css from "./{{ name }}.module.scss";


const {{ name }} = (props) => {
    return (
        <div className={cn()}>
        </div>
    );
};


export default {{ name }};
