import * as React from "react";


export interface I{{ name }} {
}


const {{ name }}: React.FC<I{{ name }}> = (props) => {
    return (
        <div className="{{ name }}">
        </div>);
};


export default {{ name }};
