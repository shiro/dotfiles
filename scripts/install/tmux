#!/bin/zsh
source ${DOTFILES}/scripts/install/_functions


this_is_called 'tmux'


command_exists $NAME
[ $? -eq 0 ] && return 0


if we_are_on 'Debian' \
|| we_are_on 'Arch'
then
  unsupported_platform
fi
