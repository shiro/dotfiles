import * as React from "react";


export interface I{{ name }} {
}


const HelloWorld: React.FC<I{{ name }}> = (props) => {
    return (
        <div className="{{ name }}">
        </div>);
};


export default {{ name }};
